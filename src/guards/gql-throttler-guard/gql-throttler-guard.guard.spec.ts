import { GqlThrottlerGuardGuard } from './gql-throttler-guard.guard';

describe('GqlThrottlerGuardGuard', () => {
  it('should be defined', () => {
    expect(new GqlThrottlerGuardGuard()).toBeDefined();
  });
});
