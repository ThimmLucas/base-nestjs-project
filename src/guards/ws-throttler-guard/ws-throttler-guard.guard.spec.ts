import { WsThrottlerGuardGuard } from './ws-throttler-guard.guard';

describe('WsThrottlerGuardGuard', () => {
  it('should be defined', () => {
    expect(new WsThrottlerGuardGuard()).toBeDefined();
  });
});
