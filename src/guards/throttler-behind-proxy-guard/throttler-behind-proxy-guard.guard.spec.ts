import { ThrottlerBehindProxyGuardGuard } from './throttler-behind-proxy-guard.guard';

describe('ThrottlerBehindProxyGuardGuard', () => {
  it('should be defined', () => {
    expect(new ThrottlerBehindProxyGuardGuard()).toBeDefined();
  });
});
